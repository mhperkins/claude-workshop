{
  "name": "claude-workshop",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix app-ui\"",
    "install:all": "npm install --prefix server && npm install --prefix app-ui"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
